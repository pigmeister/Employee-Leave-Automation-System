<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leaves</title>
</head>
<body>
    <% var i = 1 %>
    <% leaves.forEach((e) => { %>
        <tr class="table-row">
            <th scope="row"><%=i%></th>
            <% i = i + 1 %>
            <td><%= e.name %></td>
            <td><%= e.startTime%></td>
            <td><%= e.endTime%></td>
            <form method="POST" class="mt-3" id="info-form">
                <input type="hidden" id="id" name="_id" value="<%= e._id%>">
                <div class = "form-group">
                    <input type="submit" name="status" value="Approve" class="btn btn-accent-site">
                    <input type="submit" name="status" value="Reject" class="btn btn-outline-danger">
                </div>
            </form>
        </tr>
    <% }) %>
</body>
</html>